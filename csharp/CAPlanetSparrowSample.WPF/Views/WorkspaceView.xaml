<UserControl x:Class="CAPlanetSparrowSample.WPF.Views.WorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CAPlanetSparrowSample.WPF.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:WorkspaceViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding StatusText}" FontSize="14" FontWeight="Bold"/>
        <ProgressBar Grid.Row="1" Height="20" Margin="0,8" Value="{Binding Progress}"
                     Minimum="0" Maximum="100"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,4">
            <TextBlock Text="Density: "/>
            <TextBlock Text="{Binding Density, StringFormat=P2}" FontWeight="Bold"/>
            <TextBlock Text="  Width: " Margin="16,0,0,0"/>
            <TextBlock Text="{Binding StripWidth, StringFormat=F2}" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>
