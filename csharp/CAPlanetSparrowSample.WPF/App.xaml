<Application x:Class="CAPlanetSparrowSample.WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:docking="http://schemas.actiprosoftware.com/winfx/xaml/docking"
             xmlns:local="clr-namespace:CAPlanetSparrowSample.WPF">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <Style x:Key="DockSiteStyle"
                           BasedOn="{StaticResource {x:Type docking:DockSite}}"
                           TargetType="{x:Type docking:DockSite}">
                        <Setter Property="ToolWindowsHaveOptionsButtons" Value="False" />
                        <Setter Property="ToolWindowsHaveTabImages" Value="True" />
                        <Setter Property="ToolWindowsHaveTitleBarIcons" Value="True" />
                        <Setter Property="FloatingToolWindowContainersHaveMinimizeButtons" Value="False" />
                        <Setter Property="FloatingToolWindowContainersHaveMaximizeButtons" Value="False" />
                    </Style>

                    <Style x:Key="DockingDocumentWindowStyle" TargetType="docking:DocumentWindow">
                        <Setter Property="Description" Value="{Binding Path=Description}" />
                        <Setter Property="ImageSource" Value="{Binding Path=ImageSource}" />
                        <Setter Property="IsActive" Value="{Binding Path=IsActive, Mode=TwoWay}" />
                        <Setter Property="IsOpen" Value="{Binding Path=IsOpen, Mode=TwoWay}" />
                        <Setter Property="IsSelected" Value="{Binding Path=IsSelected, Mode=TwoWay}" />
                        <Setter Property="SerializationId" Value="{Binding Path=SerializationId}" />
                        <Setter Property="Title" Value="{Binding Path=Title}" />
                        <Setter Property="WindowGroupName" Value="{Binding Path=WindowGroupName, Mode=TwoWay}" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="BorderBrush" Value="Transparent" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="StandardMdiWindowState" Value="Maximized" />
                    </Style>

                    <DataTemplate x:Key="DockingDocumentItemContentTemplate">
                        <ContentControl Content="{Binding Content}" InputMethod.IsInputMethodEnabled="False" FocusVisualStyle="{x:Null}"/>
                    </DataTemplate>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
         
    </Application.Resources>
</Application>
